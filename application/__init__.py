"""
Capa de aplicación — casos de uso (orquestación).

- Coordina dominio + puertos (repositorios, servicios externos).
- Sin lógica HTTP; retorna DTOs o resultados explícitos.
- Un caso de uso por módulo/archivo cuando sea posible (ej. cobrar_vale.py).
"""
